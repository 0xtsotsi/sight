node_modules/
dist/
release/
.DS_Store

# Signing material and credentials — never commit these
.env
.env.*
*.p12
*.pfx
*.pem
*.key
*.mobileprovision

# Local agent-loop working artifacts (created by the chat assistant at
# runtime — never commit):
.gg/
CLAUDE.md
# docs/ is mostly internal notes; the screenshots subdir is committed
# under docs/screenshots/ for milestone verification.
docs/*
!docs/screenshots/
