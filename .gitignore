node_modules/
dist/
release/
.DS_Store

# Signing material and credentials — never commit these
.env
.env.*
*.p12
*.pfx
*.pem
*.key
*.mobileprovision

# Local agent-loop working artifacts (created by the chat assistant at
# runtime — never commit):
.gg/
docs/
CLAUDE.md

# Test scaffolding — generated at first run by jsdom-backed tests
# (src/panels/__tests__/AgentPanel.test.js is the first consumer).
# Never commit; safe to delete.
src/**/__tests__/_stub_*.mjs
src/**/__tests__/_*.compiled.mjs
src/**/__tests__/_*.preview.mjs
