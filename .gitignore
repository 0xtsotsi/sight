node_modules/
dist/
release/
.DS_Store

# Signing material and credentials — never commit these
.env
.env.*
*.p12
*.pfx
*.pem
*.key
*.mobileprovision

# Local agent-loop working artifacts (created by the chat assistant at
# runtime — never commit):
.gg/
docs/
CLAUDE.md
